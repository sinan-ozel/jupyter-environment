# jupyter-environment
A simple Jupyter environment using Docker & make

## Requirements
* Docker
* make

## To Get Started
Run `make build`.
Then run `make start`.

This will start the notebook server.
